RewriteEngine On
# If your folder is 'mydomain.com/api', use /api/
# If it is in the root, use /
RewriteBase /api/

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ index.php [QSA,L]
